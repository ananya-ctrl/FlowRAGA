{
  "name": "flowraga",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev:web": "npm run dev --workspace @flowraga/web",
    "build:web": "npm run build --workspace @flowraga/web",
    "lint:web": "npm run lint --workspace @flowraga/web",
    "test:web": "npm run test --workspace @flowraga/web"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}

